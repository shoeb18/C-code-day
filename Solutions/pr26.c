
//? question 26:
//* Make a program to check string length

#include <stdio.h>
#include <string.h>

int main(){

    char name[] = "Vishal Bangale";

    int length = strlen(name);

    printf("The length of string is %d\n", length);
    return 0;
}